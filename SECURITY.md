# Security Policy

## 安全模型:三层防线(Three-Layer Defense)

Vibe-Skill-Ops 是 **Markdown 文档(skill)**,不执行代码,但它在**指导 Agent 写代码** —— 所以安全重点是:让被指导的 Agent 写出安全的代码,而不是自己成为攻击面。

| 层 | 机制 | 成本 | 触发 |
|----|------|------|------|
| **L1 静默纪律** | 8 条写码纪律注入(code-security):SQL 参数化 / 密钥环境变量 / 输出转义 / 命令列表参数 / 路径白名单 / 异常记录 / 集合判空 / 输入校验 | 零(默认生效) | 写码即生效 |
| **L2 轻量扫描** | 提交闸门:semgrep 增量 diff + 依赖审计(security-audit) | 低 | 任务收尾/commit |
| **L3 深度扫描** | 全量扫描 + 闭环重扫;AI 审查引擎(open-code-review) | 高(按需) | 用户显式要求 |

**🔴 不可降级红线**(任何情况下不得关闭):
1. 密钥必须走环境变量,禁止硬编码
2. SQL 必须参数化,禁止字符串拼接
3. 用户输入输出必须转义,禁止裸拼 HTML
4. 危险命令必须有确认闸门(授权闸门,见 reverse-ops Scope Gate)

## 报告漏洞

发现安全漏洞(全家桶文档本身、或全家桶指导写出的代码存在系统性缺陷):

- **普通问题**:开 GitHub Issue,标题带 `[SECURITY]` 前缀,描述:影响面 / 复现 / 建议修复。
- **高危/紧急**(密钥泄露、命令注入、鉴权绕过):**不要公开开 Issue** —— 直接发邮件至项目维护者(见 GitHub 主页),或先私信维护者再补公开 Issue。

报告时请包含:
1. 影响版本
2. 复现步骤(最小例子)
3. 影响分析(谁受影响、多严重)
4. 建议修复(如果有)

## 处理流程

1. 确认漏洞 → 24h 内回复报告者
2. 修复 → 提交时过双闸门(hook 拦截回归)
3. 复盘 → 漏洞按 CWE 分类沉淀进 `vuln-memory`(反例→正例+教训),防止同类再犯
4. 公开 → 修复合并后,公开漏洞描述 + 修复说明

## 已知边界(诚实声明)

- **可靠性上限**:全家桶的"安全"依赖 Agent 的指令遵循能力 —— 文档不能强制 Agent 执行纪律,只能约束+扫描+审查。**工具检测是兜底,纪律才是根本防线**。
- **扫描工具边界**(实测):semgrep 对 PowerShell 0 规则命中;密钥类检测弱(不识别 `sk-`/`ghp_` 格式);裸 Python 函数 XSS/命令注入不触发 —— 这些靠 L1 纪律兜底。
- **不在范围内**:非全家桶代码、未授权系统的渗透测试(后者见 reverse-ops 授权闸门,未授权禁止 ACT)。

## 依赖安全

全家桶本体零依赖(纯 Markdown);`external/` 里的外部 skill 副本仅供分发参考,安全责任归各自上游。安装工具(ocr/semgrep 等)请通过官方渠道 + 锁定版本。
